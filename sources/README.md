# IOReg: Sources directory

## Purpose

One json file per "source" (e.g. organization, individual, etc) providing data.  Source ID should be a (unique) UUID. This ID is
in turn used to name data files under individual IDs as well.
